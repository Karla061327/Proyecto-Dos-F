{
  "name": "proyecto-dos-f",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsnd --respawn --clear public/app.js",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "author": "karla",
  "license": "ISC",
  "description": "frontend del proyecto numero dos ",
  "dependencies": {
    "@types/node": "^22.7.8",
    "cors": "^2.8.5",
    "nodemon": "^3.1.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
